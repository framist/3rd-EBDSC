# EBDSC 第三届电磁大数据非凡挑战赛方案说明

## 基本信息
- 队伍：QiiQ（西安电子科技大学）
- 演示日期：2025 年 4 月 11 日
- 主题串联：破解之道 → 初战告捷 → 双剑合璧 → 制胜一击 → 超越竞赛

## Part 01 破解之道：揭开竞赛背后的数据密码
- **任务挑战**：现代雷达通信系统采用多样调制类型，存在信号失真、低信噪比、多径传播等复杂电磁环境问题，需要在开集场景下同时完成调制类型识别、码宽回归、码序列解调，并对未知调制信号实现拒识。
- **传统解调流程瓶颈**：IQ 输入需经过载波恢复、符号定时恢复、星座映射等多阶段处理，抗失真能力弱，低信噪比下性能骤降，多径适应性差且缺乏拒识能力。
- **数据集洞察**：样本普遍噪声大且失真严重；IQ 长度及码序列分布范围广且存在长尾，码宽单位有少量异常值，最大频谱分量偏移明显。

## Part 02 初战告捷：创新模型的锋芒与盲点
- **问题规约**：将不定长码宽的解调转化为对每个 IQ 采样点进行码元类别分类，并引入中心比例掩码及派生码序列用于后处理聚合。
- **ModernTCN_MutiTask 架构**：在 `ModernTCN.py` 中实现，包含可选的值嵌入（`stem`）与 24 层 `Stage` 主干。每个 `Block` 使用可重参数化的大核卷积 (`ReparamLargeKernelConv`) 与双层 Conv-FFN，跨变量（M 维）与特征维（D 维）提取时域上下文。输出端设置三头任务：
  - `sortinghead`：对展平的 `[B, L, M·D]` 表示进行逐时间步线性层映射，实现码元分类；
  - `mod_classifier`：使用注意力池化聚合全局特征并预测调制类型；
  - `symbol_width_regressor`：共享聚合特征做码宽回归。
- **阶段性结果**：多任务头之间互补，在验证集获得 MT≈65、SW≈93、CQ≈15 分。
- **主要问题**：
  - CQ 解调性能不足，高噪声下难以提取稳定码元特征；
  - 频域特征捕捉能力有限，缺乏先验知识支撑；
  - 推理过程中误差累计、自回归效率低。
- **改进方向**：尝试引入传统解调流程，为模型提供可利用的先验信息。

## Part 03 双剑合璧：当创新遇见经典的火花
- **传统解调流程回归**：对信号执行下变频、滤波、时频变换与码宽估计（1/SW），作为网络可借鉴的先验。
- **ModernTCN_FTDW**：`ModernTCN_FTDW.py` 在主干 `Block` 内叠加频域建模单元：
  - 先将 `[B, M=2, D, T]` 的 IQ 表示转为复数并执行 FFT；
  - 通过成对的深度可分离大核卷积（实部 `fdw_real`、虚部 `fdw_imag`）建模频域邻域；
  - 复域两层 MLP (`FreMLP_complex1/2`) 与 SoftShrink 稀疏化抑制噪声；
  - iFFT 恢复时域，再与原有 ModernTCN 主干交互。
- **性能对比（验证集）**：
  - Baseline1：MT 65 / SW 93 / CQ 15
  - Baseline2：MT 70 / SW 93 / CQ 20
- **残留局限**：基频判定粗浅、缺乏全局感受野与频域建模能力，系统仍非端到端。

## Part 04 制胜一击：深度跨域重构问题边界
- **整体策略**：
  1. 第一阶段（ModernTCN_MutiTask）完成基础时域特征提取。
  2. 第二阶段（ModernTCN_FTDW）注入频域反馈，补偿参数偏差。
  3. 第三阶段（ModernTCN_FreTS）在时、频域联合建模，针对 IQ 信号引入特化的频域学习单元，解决频率偏移与相位旋转。
- **ModernTCN_FreTS 架构特点**：延续可重参数化大核卷积与多任务头，同时在每个 `Block` 内前置 `FreTS_Block`，以全局频域 MLP 提升建模能力，结构端到端、模块化，可按需启用不同嵌入（固定、可学习或携带位置信息）。
- **FreTS Block 工作流程**：
  1. IQ 序列经 FFT/rFFT 转入复频域；
  2. 使用复权重 `FreMLP`/`FreMLP_complex1-2` 构造的频域 MLP，并配合 SoftShrink 稀疏激活抑制噪声；
  3. 经线性映射回到原嵌入维；
  4. 通过 iFFT 回到 IQ 域，与原始特征残差相加形成跨域融合。
- **复域计算公式**：
  \[
  Y^\ell = \sigma\big(\operatorname{Re}(Y^{\ell-1})W_r^\ell - \operatorname{Im}(Y^{\ell-1})W_i^\ell + B_r^\ell\big) + j\,\sigma\big(\operatorname{Re}(Y^{\ell-1})W_i^\ell + \operatorname{Im}(Y^{\ell-1})W_r^\ell + B_i^\ell\big),
  \]
  其中 \(W^\ell = W_r^\ell + jW_i^\ell\)，\(B^\ell = B_r^\ell + jB_i^\ell\)。
- **稀疏激活**：SoftShrink 在频域内执行阈值化，保留主要频谱成分并抑制噪声。
- **提交与迭代要点**：
  - ModernTCN_FreTS 结构引入频域建模能力；
  - 基于置信度的开集学习增强拒识能力；
  - 随机频谱偏移投票机制提升最终表现；
  - 最终提交成绩：MT 77.04、SW 97.22、CQ 34.25（初赛第三名）。

## Part 05 超越竞赛：技术落地与未来可能性
- **技术总结**：
  - 在零数据增强与零数据清洗的前提下实现广域样本适应；
  - 提出变长码宽解调的任务规约方法；
  - 构建适配 IQ 信号的复频域深度学习模块；
  - 给出未知调制类型的拒识策略。
- **未来规划**：
  - 进一步进行领域特化增强，与经典解调流水线深度融合；
  - 探索更合理的开集识别框架；
  - 研究能够联合提取时域、空域、频域、值域与特征域信息的通用方法。
- **愿景**：致力于洞悉问题本质，提供可落地的实用方案，而非构建“空中楼阁”。

## 参考文献
1. "ModernTCN: A Modern Pure Convolution Structure for General Time Series Analysis," The Twelfth International Conference on Learning Representations, 2023.
2. Yi K., Zhang Q., Fan W., et al. *Frequency-domain MLPs are More Effective Learners in Time Series Forecasting*. arXiv:2311.06184, 2023.
3. Samer Hanna, Chris Dick, Danijela Cabric. *Signal Processing Based Deep Learning for Blind Symbol Decoding and Modulation Classification*, 2021.
4. Xianan Hu, Fu Li, Kairui Niu, Peihan Qi, Zhiyong Liang. *WVEmbs with its Masking: A Method For Radar Signal Sorting*, 2025.
